# UD-IQ3_XXS with Open WebUI

```bash
./start.sh
./health_check.sh
./stop.sh
```

This is a general chat UI with a dedicated persistent volume. It does not set a
SQL-specific system prompt. Use `./build.sh` only when the llama.cpp image must
be rebuilt and the pinned source/base image are available.
